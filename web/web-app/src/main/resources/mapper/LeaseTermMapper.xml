<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.app.mapper.LeaseTermMapper">


    <select id="customListByRoomId" resultType="com.atguigu.lease.model.entity.LeaseTerm">
--         根据房间id查询他所有的租期
        select lt.*
        from
            (select * from room_lease_term rlt where rlt.room_id = #{id} and rlt.is_deleted = 0) t1
                left join
            lease_term lt
            on t1.lease_term_id = lt.id and lt.is_deleted = 0;
    </select>
</mapper>
