<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.app.mapper.PaymentTypeMapper">


    <select id="customListByRoomId" resultType="com.atguigu.lease.model.entity.PaymentType">
        select pt.*
        from
            (select * from room_payment_type rpt where rpt.room_id = 3 and rpt.is_deleted = 0) t1
                left join
            payment_type pt
            on
                t1.payment_type_id = pt.id and pt.is_deleted = 0
    </select>
</mapper>