<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.app.mapper.AttrValueMapper">

    <resultMap id="AttrValueVo" type="com.atguigu.lease.web.app.vo.attr.AttrValueVo">
        <id column="id" property="id"></id>
    </resultMap>

    <select id="customAttrValueVoList" resultMap="AttrValueVo">
        select
            av.*,
            ak.name attr_key_name
        from
            (select * from room_attr_value rav where rav.room_id = #{id} and rav.is_deleted = 0) t1
                left join
            attr_value av
            on
                t1.attr_value_id = av.id and av.is_deleted = 0

                left join
            attr_key ak
            on
                av.attr_key_id = ak.id and ak.is_deleted = 0;
    </select>
</mapper>
