<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.admin.mapper.ApartmentFacilityMapper">

    <select id="listFacilityInfoByApartmentId" resultType="com.atguigu.lease.model.entity.FacilityInfo">
        SELECT fi.* FROM
            ( SELECT *
              FROM apartment_facility af where af.apartment_id=#{id} and af.is_deleted=0
            ) t1
                LEFT JOIN facility_info fi
                          on t1.facility_id=fi.id
        where fi.is_deleted=0;
    </select>
</mapper>
