<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.admin.mapper.ApartmentLabelMapper">

    <select id="listLabelInfoByApartmentId" resultType="com.atguigu.lease.model.entity.LabelInfo">
        select label_info.*
        from (
                 select al.label_id from apartment_label al where al.apartment_id = #{id} and al.is_deleted=0
             ) t1
                 left join
             label_info
             on t1.label_id = label_info.id
        where label_info.is_deleted=0;

    </select>
</mapper>
