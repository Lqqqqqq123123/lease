<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.admin.mapper.SystemUserMapper">

    <select id="customPage" resultType="com.atguigu.lease.web.admin.vo.system.user.SystemUserItemVo">
        select su.*, sp.name as post_name
        from
            system_user su
                left join
            system_post sp
            on su.post_id = sp.id and su.is_deleted = 0 and sp.is_deleted = 0
        <trim prefix="where" prefixOverrides="and | or">
            <if test="queryVo.name != null">
                and su.username      like concat('%', #{queryVo.name}, '%')
            </if>
            <if test="queryVo.phone != null">
                and su.phone like concat('%', #{queryVo.phone}, '%')
            </if>
        </trim>
    </select>

    <select id="customGetDetailById" resultType="com.atguigu.lease.web.admin.vo.system.user.SystemUserItemVo">
        select su.*, sp.name as post_name
        from
        system_user su
        left join
        system_post sp
        on su.post_id = sp.id and su.is_deleted = 0 and sp.is_deleted = 0
        where su.id=#{id}
    </select>
</mapper>
